<div class="post">
				<div class="bg1">
					<div class="bg2">
						<div class="bg3">
						
<h2 class="title">Listindo tipos</h2>

<table>
  <tr>
    <th>Type name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @types.each do |type| %>
  <tr>
    <td><%= type.type_name %></td>
    <td><%= link_to 'Show', type %></td>
    <td><%= link_to 'Edit', edit_type_path(type) %></td>
    <td><%= link_to 'Destroy', type, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />
						</div>
					</div>
				</div>
</div>
<div id="label">
<%= link_to 'Nuevo tipo', new_type_path %>
</div>