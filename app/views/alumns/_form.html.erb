

<%= form_for(@alumn) do |f| %>
  <% if @alumn.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@alumn.errors.count, "error") %> prohibited this alumn from being saved:</h2>

      <ul>
      <% @alumn.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id, "Alumno" %><br />
    <%= collection_select(:alumn, :user_id, @alumns, :id, :username) %>
  </div>
  <div class="field">
    <%= f.label :course_id, "Curso" %><br />
    <%= collection_select(:alumn, :course_id, @cursos, :id, :curso) %>
  </div>
  <div class="actions">
    <%= f.submit "Enviar" %>
  </div>
<% end %>
